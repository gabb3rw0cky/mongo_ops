/**
 * A set of properties for a customizable card component.
 * @typedef {object} CardProps
 * @property {React.ReactNode} children - The content inside the card.
 */
export type     CardProps   =   {
    children        :   React.ReactNode
}